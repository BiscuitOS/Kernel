#
# Common System Call
#

menu "System CAll"

config TESTCASE_SYSCALL
	bool "System CAll"
	help
	  System Call

if TESTCASE_SYSCALL

config TESTCASE_SYSCALL_OPEN
	bool "SYSTEM CALL: open"
	help
	  System call routine for open

config TESTCASE_SYSCALL_ROUTINE
	bool "System CAll common routine"
	help
	  The routine of system call on IDT, GDT and LDT.

endif

endmenu
